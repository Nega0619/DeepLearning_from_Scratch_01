import numpy as np

def numerical_differentiation(f, x):
    h = 1e-4
    
    for idx in range(x.size):
        x[idx] = 


def numerical_gradient(f, init_x, lr=0.1, num_steps=100):

    grad = 
    x = x -lr *  grad


